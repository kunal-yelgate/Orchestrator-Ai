import React from "react";

const GraphCanvas = () => {
  return (
    <section
      style={{
        padding: "1rem",
        border: "1px solid #e5e7eb",
        borderRadius: "12px",
      }}
    >
      <h3 style={{ marginBottom: "0.5rem" }}>Graph Canvas</h3>
      <p style={{ margin: 0, color: "#6b7280" }}>
        This is where your workflow graph will render.
      </p>
    </section>
  );
};

export default GraphCanvas;
